# 动作数据集 JSON 生成器

## 项目概述

动作数据集 JSON 生成器是一个为**光学动作捕捉数据采集**服务的训练数据集生成工具。它能够批量生成符合规范的动作指令 JSON 数据，涵盖 16 个动作类别，支持两种生成模式：基于 DeepSeek 大模型的智能生成和基于本地要素池的规则组合生成。

## 技术架构

| 层级 | 技术 | 说明 |
|------|------|------|
| 前端 | Vue 3 + Vite | SPA 单页应用，提供可视化的配置、生成、预览、下载界面 |
| 后端 | Python FastAPI | RESTful API 服务，负责生成调度、数据校验、持久化 |
| 数据库 | SQLite (aiosqlite) | 本地轻量数据库，WAL 模式，存储去重池、历史记录、关键词池 |
| LLM | DeepSeek API (deepseek-chat) | 可选的 LLM 生成模式，创造多样化的动作数据 |

## 功能特性

### 1. 双模式生成

- **LLM 模式**：配置 `DEEPSEEK_API_KEY` 环境变量后启用，调用 DeepSeek 大模型创造全新、不模板化的动作数据，支持并发请求（12 并发）和批量生成（每次 3-5 条）
- **要素池模式**（兜底）：无需 API Key，从预定义的要素池中随机组合动作、身体部位、情绪、幅度、速度等元素生成数据，同样支持去重和校验

### 2. 16 个动作类别

站立、行走、跑步、跳跃、下蹲、特技、舞蹈、爬行、单膝跪地、互动、挪动物品、后退、侧移、踏步、上肢动作(比心)、其他

每个类别都配置了专属的动作列表、身体部位、情绪范围、语音反馈模板和 Query 模板。

### 3. 数据格式

每条生成的动作数据包含以下字段：

| 字段 | 说明 | 示例 |
|------|------|------|
| actor_id | 动作执行者标识 | `1_Skeleton0` |
| id | 序号 | `0001` |
| query | 口语化指令（5-20 字） | `来，立正` |
| query_description | 标签格式描述 | `[[立正]，[情绪:严肃]]` |
| text | 动作文本描述 | `一个人双脚并拢，全身挺直...最后回到站立姿态` |
| motion_description | 动作标签 | `[[情绪:严肃]，[动作:立正]，[次数:1次]...]` |
| is_head | 是否仅头部动作 | `true` / `false` |
| voice_feedback | 语音反馈（13-22 字） | `立正完毕，随时待命！` |
| category | 动作类别 | `站立` |
| intent | 意图标签 | `单意图动作` |
| aug_text | 6 个文本变体 | 从极简到详细的 6 个版本 |

### 4. 数据校验

三层校验流水线确保数据质量：

1. **格式校验**：字段存在性、类型、长度、标签格式合法性
2. **内容合法性**：动作次数/步数限制、部位动作匹配、物理可行性、安全伦理约束、情绪-速度一致性
3. **去重校验**：与历史去重池和当前批次去重

### 5. 批量生成与导出

- 支持单次生成（最多 50,000 条/文件）和批量生成（最多 50 个文件）
- 单文件 JSON 下载
- 多文件批量 ZIP 打包下载
- 历史记录保存与重新生成

### 6. 数据导入

支持导入 JSON 文件，将 query 写入去重池，避免后续生成重复数据。

## 项目结构

```
json-generator-web/
├── backend/
│   ├── main.py              # FastAPI 入口 + API 路由
│   ├── models.py            # Pydantic 数据模型
│   ├── generator.py         # 生成调度器（LLM/要素池分发）
│   ├── llm_client.py        # DeepSeek LLM 客户端
│   ├── element_pool.py      # 要素池定义（关键词库）
│   ├── element_generator.py # 要素池组合生成器
│   ├── validator.py         # 三层校验流水线
│   ├── database.py          # SQLite 数据库操作
│   └── requirements.txt     # Python 依赖
├── frontend/
│   ├── index.html           # 入口 HTML
│   ├── vite.config.js       # Vite 配置（含 API 代理）
│   ├── package.json         # 前端依赖
│   └── src/
│       ├── main.js          # Vue 应用入口
│       ├── App.vue          # 主组件
│       ├── api/index.js     # API 请求封装
│       ├── styles/global.css
│       └── components/
│           ├── CategorySelector.vue  # 类别选择器
│           ├── WeightInput.vue       # 权重输入
│           ├── CountInput.vue        # 条数输入
│           ├── BatchPanel.vue        # 批量生成面板
│           ├── GenerateButton.vue    # 生成按钮
│           ├── PreviewTable.vue      # 数据预览表格
│           ├── DownloadButton.vue    # 下载按钮
│           ├── ImportButton.vue      # 导入按钮
│           └── HistoryPanel.vue      # 历史记录面板
├── start.bat               # 一键启动脚本
└── check_env.bat           # 开发环境检查脚本
```

## 快速开始

### 环境要求

- Python 3.10+
- Node.js 18+
- （可选）Ollama + qwen3:4b 模型 / DeepSeek API Key

### 启动步骤

1. **检查环境**
   ```bash
   check_env.bat
   ```

2. **安装后端依赖**
   ```bash
   cd backend
   pip install -r requirements.txt
   ```

3. **安装前端依赖**
   ```bash
   cd frontend
   npm install
   ```

4. **启动服务**（一键启动）
   ```bash
   start.bat
   ```
   或者分别启动：
   ```bash
   # 后端（端口 8000）
   cd backend && python main.py

   # 前端（端口 5173）
   cd frontend && npx vite --host
   ```

5. **访问应用**：打开浏览器访问 `http://localhost:5173`

### 启用 LLM 模式

在 `backend/` 目录下创建 `.env` 文件：
```
DEEPSEEK_API_KEY=your_api_key_here
```
注册地址：https://platform.deepseek.com

## API 接口

| 方法 | 路径 | 说明 |
|------|------|------|
| GET | `/api/status` | 检查 LLM 可用性与当前模式 |
| POST | `/api/generate` | 单次生成 JSON 数据 |
| POST | `/api/generate/batch` | 批量生成多个 JSON 文件 |
| GET | `/api/history` | 获取历史生成记录 |
| DELETE | `/api/history/{id}` | 删除历史记录 |
| POST | `/api/history/{id}/regenerate` | 用历史参数重新生成 |
| POST | `/api/import` | 导入 JSON 文件到去重池 |
| GET | `/api/query-pool/stats` | 查看去重池统计信息 |

API 文档（Swagger UI）：`http://localhost:8000/docs`
